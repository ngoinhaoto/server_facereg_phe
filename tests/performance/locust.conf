
host = http://localhost:8000

# Number of users to simulate
users = 20

# How fast to spawn users (users per second)
spawn-rate = 2

# Run time limit
run-time = 5m

# Set headless mode to true for running without the web UI
headless = false

# For headless mode, you can specify a CSV file for results
# csv = locust_results